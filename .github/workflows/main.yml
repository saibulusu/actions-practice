on:
  pull_request:
    types:
      - closed

jobs:
  if_merged:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    working-directory: ${GITHUB_WORKSPACE}
    steps:
      - run: |
          chmod u+x ./update-version
          ./update-version
